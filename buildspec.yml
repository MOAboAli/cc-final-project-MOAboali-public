version: 0.2

phases:
  install:
    commands:
      - npm install -g @angular/cli
      - npm install
  build:
    commands:
      - ng build --output-path=dist/frontend/browser

artifacts:
  files:
    - dist/frontend/browser/**/*
